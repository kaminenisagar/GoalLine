# ─── Vite environment variables (must be prefixed VITE_) ─────────
# Local dev: leave empty — Vite proxy handles /api → localhost:5005
VITE_API_URL=

# Production (Vercel): set this to your Render backend URL
# VITE_API_URL=https://goalline-api.onrender.com/api
